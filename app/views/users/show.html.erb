<div class="row justify-content-between">
    <div class="col-8">
      <h2><%= @user.name %>'s profile</h2>
    </div>
    <div class="col-4 text-end">
      <% if @can_send_friend_request %>
        <%= button_to "Send friend request", add_friend_path, class: 'btn btn-primary', method: :put %>
    <% end %>
    </div>
</div>

